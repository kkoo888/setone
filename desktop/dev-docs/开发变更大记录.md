# 开发变更大记录

## 2026-05-20 环境初始化

### SSH 密钥生成
- 生成 Ed25519 SSH 密钥对，用于 GitHub 认证
- 公钥已由用户手动添加到 GitHub 账户

### Skill 安装
新增 5 个 skill，增强小希能力：
| Skill | 用途 |
|-------|------|
| skill-vetter | 安装前审查第三方技能的安全性 |
| self-improving-agent | 持续自我学习和改进 |
| self-improving | 自我反思与错误修正 |
| humanizer | 去除 AI 写作痕迹，使文本更自然 |
| github | 使用 gh CLI 操作 GitHub（issue/PR/CI） |

### 仓库克隆
- 克隆 `setone` 仓库 `dev` 分支
- 路径：`/root/.openclaw/workspace/setone`

### 定时任务
- 设置 55 分钟后自动提交工作区变更
- 自动推送到 dev 分支

## 2026-05-20 主题系统优化（字号梯度重整）

### 变更概述
借鉴 shadcn/ui、Ant Design、Mantine 等高星 UI 框架，对主题系统进行系统性优化。

### 字号梯度体系（新增）
| Token | 值 | 用途 |
|-------|-----|------|
| --font-size-2xs | 10px | 辅助标签、徽章 |
| --font-size-xs | 12px | 时间戳、说明文字 |
| --font-size-sm | 14px | 正文（桌面应用默认） |
| --font-size-md | 16px | 输入框、按钮 |
| --font-size-lg | 18px | 小标题 |
| --font-size-xl | 20px | 页面标题 |
| --font-size-2xl | 24px | 模块标题 |
| --font-size-3xl | 32px | 数字展示 |

### 核心变更
1. themeEngine.ts：字号从倍率派生改为固定整数梯度
2. 26 个 CSS 文件：硬编码 font-size/padding/border-radius 替换为 CSS 变量
3. button.css：新增 .btn-icon、.btn-pill 统一按钮变体
4. variables.css：清理 --font-size-md-lg 中间变量
5. 安装 3 个设计系统 skill：distinctive-design-systems、frontend-architecture-pro、design-system-patterns

### 设计借鉴
- shadcn/ui：三层 token 架构（primitive → semantic → component）
- Ant Design：fontSize 作为 seed token
- Mantine：统一字号梯度命名
- distinctive-design-systems：美学文档优先、type scale 乘数器

## 2026-05-20 开发环境重新配置（21:39）

### Skill 重新安装
重新安装 5 个 skill 到 workspace：
| Skill | 用途 |
|-------|------|
| skill-vetter | 安装前审查第三方技能的安全性 |
| self-improving-agent | 持续自我学习和改进 |
| self-improving | 自我反思与错误修正 |
| humanizer | 去除 AI 写作痕迹，使文本更自然 |
| github | 使用 gh CLI 操作 GitHub |

### SSH 密钥
- 生成新 Ed25519 密钥对（id_ed25519_openclaw）
- 配置 SSH config 指向新密钥
- 公钥已由用户添加到 GitHub

### 仓库配置
- 克隆 setone 仓库，切换到 dev 分支
- 工作目录：/root/.openclaw/workspace/setone
- 开发内容目录：desktop/

### 自动提交
- 设置 55 分钟后定时提交任务
- 提交流程：pull → 处理冲突 → add → commit → push 到 dev

## 2026-05-20 UI/UX 分析与默认主题设置（21:45）

### Skill 安装
- 新增安装 ui-ux-pro-max skill（评分 4.429）

### 默认主题设置
- 修改 useSettingsStore.ts：默认 theme 从 'system' → 'light'
- 修改 useTheme.ts：无保存主题时自动加载 'default' 主题
- 确保应用启动时始终使用"默认主题"

### UI/UX 分析结果
发现的主要问题：
| 问题 | 数量 | 优先级 |
|------|------|--------|
| padding/margin 硬编码 | 326 处 | 高 |
| font-size 硬编码 | 19 处 | 高 |
| border-radius 硬编码 | 32 处 | 高 |
| 硬编码颜色 | ~12 处 | 中 |
| 按钮系统不统一 | - | 高 |
| 暗色 fallback 不一致 | - | 中 |

提出 7 项优化建议待执行。

## 2026-05-20 CSS 全面重构（21:51）

### 硬编码值替换
- global.css: 多处硬编码 px → CSS 变量
- chat.css: 多处硬编码间距/字号/圆角 → CSS 变量
- button.css: 按钮系统 v2.0 统一（primary/secondary/ghost/danger/icon/pill 六变体 + sm/md/lg 三尺寸）

### 暗色模式修复
- variables.css: fallback 色值与 themeEngine GRAY_SCALE_DARK 对齐

### 无障碍增强
- 添加 focus-visible 统一样式
- 添加 prefers-reduced-motion 支持

### 深入组件分析发现
- ChatPage: 内联回调未 memoize、ErrorBoundary 缺少 aria-live
- SettingsPage: label 缺少 htmlFor、window.confirm 不够无障碍
- StatusBar: 轮询间隔硬编码、getColor 未提取、缺少 aria-live="polite"
- dashboard.css: 12+ 处硬编码 px、8+ 处硬编码 rgba 颜色、动画未适配 reduced-motion
- workflow.css: 20+ 处硬编码 px、15+ 处魔法颜色值、z-index 硬编码、动画未适配 reduced-motion

## 2026-05-20 开发环境再次重建（23:43）

### Skill 更新
新增/重新安装 6 个 skill：
| Skill | 用途 |
|-------|------|
| skill-vetter | 安装前审查第三方技能的安全性 |
| self-improving-agent | 持续自我学习和改进 |
| self-improving | 自我反思与错误修正 |
| humanizer | 去除 AI 写作痕迹，使文本更自然 |
| github | 使用 gh CLI 操作 GitHub |
| ui-ux-pro-max | UI/UX 专业分析与优化 |

### SSH 密钥
- 生成新 Ed25519 密钥对（openclaw@setone）
- 公钥已由用户添加到 GitHub

### 仓库配置
- 重新克隆 setone 仓库 dev 分支
- 工作目录：/root/.openclaw/workspace/setone
- 开发内容目录：desktop/

### 自动提交
- 设置 55 分钟后定时提交任务（00:40）
- 流程：pull → 处理冲突 → add → commit → push 到 dev

## 2026-05-21 环境重建

### Skill 重新安装
新增 6 个 skill（重新安装）：
| Skill | 用途 |
|-------|------|
| skill-vetter | 安装前审查第三方技能的安全性 |
| self-improving-agent | 持续自我学习和改进 |
| self-improving | 自我反思与错误修正 |
| humanizer | 去除 AI 写作痕迹，使文本更自然 |
| github | 使用 gh CLI 操作 GitHub（issue/PR/CI） |
| ui-ux-pro-max | UI/UX 专业设计增强 |

### SSH 密钥重新生成
- 重新生成 Ed25519 SSH 密钥对
- 指纹：SHA256:j6HNJlQ2gMRY5V8DFP3XGWzWjF3iRsonO1PQJMtQ9h4
- 用户已添加到 GitHub Deploy keys

### 仓库重新克隆
- 克隆 `setone` 仓库 `dev` 分支
- 路径：`/root/.openclaw/workspace/setone`
- Git user 配置为"小茜"
- SSH config 已配置 github.com 简化操作

### 定时任务
- 设置 55 分钟后自动提交工作区变更（约 03:21）
- 自动推送到 dev 分支

## 2026-05-21 环境重建（第三次）

### Skill 重新安装
重新安装 6 个 skill：
| Skill | 用途 |
|-------|------|
| skill-vetter | 安装前审查第三方技能的安全性 |
| self-improving-agent | 持续自我学习和改进 |
| self-improving | 自我反思与错误修正 |
| humanizer | 去除 AI 写作痕迹，使文本更自然 |
| github | 使用 gh CLI 操作 GitHub（issue/PR/CI） |
| ui-ux-pro-max | UI/UX 专业设计增强 |

### SSH 密钥
- 重新生成 Ed25519 SSH 密钥对（openclaw@setone）
- 指纹：SHA256:IuW50mDLjgsQREgUTP1frnFYWSPC+awvHAQlbhpjlrk
- 用户已添加到 GitHub Deploy keys

### 仓库配置
- 重新克隆 setone 仓库 dev 分支
- 路径：/root/.openclaw/workspace/setone
- Git user 配置为"小茜"
- SSH config 已配置 github.com 简化操作

### 定时任务
- 设置 55 分钟后自动提交工作区变更（约 20:03）
- 自动推送到 dev 分支

## 2026-05-21 Live2D5 模块路径修复与功能分析

### Core SDK 和 Shader 路径修复
- 问题：打包后 file:// 协议下绝对路径 `/lib/...` 解析到磁盘根目录
- 修复：改为 `./` 相对路径，DOM API 相对 HTML 文档解析
- cubism5-service.ts: `'./lib/live2dcubismcore5.min.js'`
- cubismshader_webgl.ts: `'./Framework/Shaders/WebGL/'`
- Live2D5PetPage.tsx: `new URL('./live2d/Hiyori/...', document.baseURI).href`

### waitForShaders 修复
- 问题：`this.renderer.waitForShaders is not a function`
- 原因：Cubism SDK 没有 waitForShaders 方法
- 修复：在 initRenderer() 中调用 renderer.loadShaders()，移除 waitForShaders 调用

### Demo vs 模块功能差异分析
缺失效果清单：
| 效果 | Demo 实现 | 模块状态 |
|------|----------|---------|
| 物理演算 | CubismPhysicsUpdater | ❌ 缺失 |
| 自动眨眼 | CubismEyeBlinkUpdater | ❌ 缺失 |
| 呼吸效果 | CubismBreathUpdater | ❌ 缺失 |
| 鼠标注视 | CubismLookUpdater | ❌ 缺失 |
| 口型同步 | CubismLipSyncUpdater | ❌ 缺失 |
| Pose 切换 | CubismPoseUpdater | ❌ 缺失 |
| 点击交互 | hitTest + 随机表情/动作 | ❌ 缺失 |
| 拖拽交互 | setDragging | ❌ 缺失 |
| UpdateScheduler | 统一管理所有 Updater | ❌ 缺失 |

### Cubism SDK 版本选型结论
- 选择 Cubism SDK for Web（TypeScript + WebGL）
- 与 Electron + React + Vite 技术栈完全匹配
- Native SDK 开发成本高 5 倍，且最终还是走 WebGL 渲染

## 2026-05-21 初始化部署（20:09）

### Skill 安装
重新安装 6 个 skill：
| Skill | 用途 |
|-------|------|
| skill-vetter | 安装前审查第三方技能的安全性 |
| self-improving-agent | 持续自我学习和改进 |
| self-improving | 自我反思与错误修正 |
| humanizer | 去除 AI 写作痕迹，使文本更自然 |
| github | 使用 gh CLI 操作 GitHub（issue/PR/CI） |
| ui-ux-pro-max | UI/UX 专业设计增强 |

### SSH 密钥
- 生成 Ed25519 SSH 密钥对用于 GitHub 仓库访问
- 用户已添加到 GitHub

### 仓库配置
- 克隆 `git@github.com:kkoo888/setone.git` 仓库
- 切换到 dev 分支
- 路径：`/root/.openclaw/workspace/setone`

### 定时任务
- 设置 55 分钟后自动提交 desktop 文件夹变更
- 自动推送到 dev 分支

### 子代理
- 启动子代理更新聊天大记录和开发变更大记录

## 2026-05-21 Live2D Cubism 5 模块重构（20:14）

### 背景
对比 Demo（LAppModel）与项目模块（cubism5-service）的实现差异，发现旧版手动使用底层 API 拼装模型，缺少大量效果：
- 物理演算、自动眨眼、呼吸、鼠标注视、Pose、LipSync、UpdateScheduler

### 决策
用户决定：删除旧代码，基于 CubismUserModel 重新开发，保持代码整洁。

### 实施
1. **新建 AppModel**（`modules/live2d-5/services/AppModel.ts`）
   - 继承 `CubismUserModel`，使用 SDK 标准加载链路
   - 使用 `CubismModelSettingJson` 解析 model3.json
   - 通过 `CubismUpdateScheduler` 统一调度所有 Updater
   - 预加载所有表情和动作到缓存

2. **重写 cubism5-service.ts**
   - 用 `AppModel` 替代手动拼装
   - 对外接口不变（loadModel/setExpression/playMotion/destroy）
   - Live2D5PetPage 无需改动

3. **新增效果**
   - CubismEyeBlinkUpdater — 自动眨眼
   - CubismBreathUpdater — 呼吸
   - CubismLookUpdater — 鼠标注视
   - CubismPhysicsUpdater — 物理演算
   - CubismPoseUpdater — Pose 切换
   - CubismLipSyncUpdater — 口型同步（预留接口）

### 文件变更
| 文件 | 操作 |
|------|------|
| `modules/live2d-5/services/AppModel.ts` | 新建 |
| `modules/live2d-5/services/cubism5-service.ts` | 重写 |
| `modules/live2d-5/CHANGELOG.md` | 新增 v2.0.0 记录 |

## 2026-05-21 Live2D5 模块 14 项优化（20:36）

### 对比 Demo 后发现的优化点
逐条验证 Demo 的 LAppModel 实现，确认 14 项优化适用。

### 实施
| # | 优化项 | 说明 |
|---|--------|------|
| 1 | Look 注视参数 | 6 个 LookParameterData，鼠标移动时模型跟随注视 |
| 2 | 呼吸参数修正 | peak 从 0.01 提升到 Demo 级别（15/8/10/4） |
| 3 | 动作关联眨眼/唇动 | setEffectIds 让动作播放时自动同步眨眼和口型 |
| 4 | loadParameters/saveParameters | 帧间参数状态保存/恢复 |
| 5 | LipSync ID 配置化 | 从 modelSetting 读取，不硬编码 |
| 6 | 待机动作循环 | Idle 组自动随机播放 |
| 7 | 点击交互 | hitTest + onTap（头→表情，身体→动作） |
| 8 | LipSync 音频 | 预留接口，暂不实现 |
| 9 | MOC 一致性校验 | 启用 mocConsistency 检查 |
| 12 | ViewMatrix | 设备坐标→模型空间坐标变换 |
| 13 | Layout | 从 model3.json 读取 Layout 配置 |
| 14 | Motion 优先级 | Force > Normal > Idle 调度机制 |
| 11 | 多模型管理 | Map<string, AppModel> + switchModel |
| - | Vite 警告 | 动态 import 改静态 import |

## 2026-05-21 小茜上线与开发环境准备

### Skill 重新安装
新增安装 3 个 skill（已有 3 个跳过）：
| Skill | 用途 |
|-------|------|
| self-improving | 自我反思、自我批评、自我学习与自组织记忆 |
| humanizer | 去除 AI 写作痕迹，使文本更自然 |
| ui-ux-pro-max | UI/UX 设计智能与实现指导 |

### SSH 密钥生成
- 生成 Ed25519 SSH 密钥对（密钥注释：openclaw@setone）
- 公钥已由用户手动添加到 GitHub 账户

### 仓库克隆与配置
- 克隆 `setone` 仓库 `dev` 分支
- 路径：`/root/.openclaw/workspace/setone`
- Git user 配置：XiaoQian / openclaw@setone

### 定时提交任务
- 设置 55 分钟后自动提交 desktop 文件夹变更到 dev 分支
- 使用 cron 定时任务实现（22:03 触发）

## 2026-05-21 Live2D5 模块优化与 bug 修复

### 模型管理 UI + unloadModel + TapBody 动态检测
**提交**: `20f8648`

| 文件 | 变更 |
|------|------|
| cubism5-service.ts | 新增 unloadModel/getLoadedModels/getActiveModelName，暴露到 window |
| AppModel.ts | TapBody 从 model3.json 动态检测，新增 _tapMotionGroup 字段 |
| Live2D5Page/index.tsx | 模型管理 Tab（列表/切换/卸载）+ 设置 Tab（配置展示） |
| index.ts (module) | 新增 3 个 IPC handler + Capabilities |
| types.ts | 补全 IPC 通道类型 |
| module.json | 新增 3 个 capability 声明 |
| live2d5.css | 模型管理面板样式 |

### CubismColorBlend 枚举 bug 修复
**提交**: `bf434b8`

| 文件 | 变更 |
|------|------|
| cubismmodel.ts | CubismColorBlend 枚举值从 Live2DCubismCore.ColorBlendType_* 改为硬编码数字 |

**根因**: 枚举值在模块加载时求值，此时 Core SDK 尚未通过 script 标签加载完成
**修复**: 使用硬编码数字值（与 Core SDK 实际值一致），避免运行时依赖

## 2026-05-21 环境重建（22:00）

### Skill 重新安装
重新安装 6 个 skill：
| Skill | 用途 |
|-------|------|
| skill-vetter | 安装前审查第三方技能的安全性 |
| self-improving-agent | 持续自我学习和改进 |
| self-improving | 自我反思、自我批评、自我学习与自组织记忆 |
| humanizer | 去除 AI 写作痕迹，使文本更自然 |
| github | 使用 gh CLI 操作 GitHub（issue/PR/CI） |
| ui-ux-pro-max | UI/UX 设计智能与实现指导 |

### SSH 密钥
- 生成新的 Ed25519 SSH 密钥对（openclaw@setone）
- 公钥已由用户手动添加到 GitHub 账户

### 仓库配置
- 克隆 `setone` 仓库 `dev` 分支
- 路径：`/root/.openclaw/workspace/setone`
- Git user 配置为"小茜"

### 定时任务
- 设置 55 分钟后自动提交工作区变更
- 自动推送到 dev 分支

## 2026-05-21 初始化开发环境

### SSH 密钥配置
- 生成 SSH 密钥对用于 GitHub 认证
- 公钥已由用户添加到 GitHub

### Skill 安装
新增 6 个 skill：
| Skill | 用途 |
|-------|------|
| skill-vetter | 安装前审查第三方技能的安全性 |
| self-improving-agent | 持续自我学习和改进 |
| self-improving | 自我反思、自我批评、自我学习与自组织记忆 |
| humanizer | 去除 AI 写作痕迹，使文本更自然 |
| github | 使用 gh CLI 操作 GitHub（issue/PR/CI） |
| ui-ux-pro-max | UI/UX 设计智能与实现指导 |

### 仓库拉取
- 克隆 `git@github.com:kkoo888/setone.git` 仓库 dev 分支
- 路径：/root/.openclaw/workspace/setone
- 重点关注 desktop 文件夹，后续开发内容放在该目录

### 定时提交任务
- 设置 55 分钟定时提交任务
- 流程：pull → 处理冲突 → add → commit → push 到 dev 分支

## 2026-05-22 开发环境重新初始化（bootstrap.sh）

### 变更概述
执行 setone 仓库 dev 分支下的 bootstrap.sh 脚本，重新初始化开发环境。

### 基础技能安装
| Skill | 用途 | 状态 |
|-------|------|------|
| skill-vetter | 安装前审查第三方技能安全性 | ✅ |
| self-improving-agent | 持续自我学习和改进 | ✅ |
| self-improving | 自我反思与错误修正 | ✅ |
| humanizer | 去除AI写作痕迹 | ✅ |
| github | gh CLI 操作 GitHub | ✅ |
| ui-ux-pro-max | UI/UX 专业设计能力 | ✅ |

### SSH 密钥配置
- 生成 Ed25519 SSH 密钥对
- 公钥：ssh-ed25519 AAAAC3NzaC1lZDI1NTE5AAAAIEHII1zmO8Uy327chXBNCrCPqKXFLpUqDm7Zl1yteI9f root@github
- 已由用户手动添加到 GitHub

### 仓库克隆
- 克隆 `setone` 仓库 `dev` 分支
- 路径：`/root/.openclaw/workspace/setone`

### GitHub 技能库安装
| 仓库 | 本地名称 | 状态 |
|------|----------|------|
| mattpocock/skills | mattpocock-skills | ✅ |
| vercel-labs/agent-skills | vercel-agent-skills | ✅ |
| obra/superpowers | superpowers | ✅ |

### 原生开发技能创建（17个）
react-best-practices、typescript-best-practices、tdd-workflow、systematic-debugging、planning-and-execution、code-review、requirements-clarification、architecture-review、vercel-react-best-practices、composition-patterns、web-design-guidelines、subagent-driven-development、dispatching-parallel-agents、verification-before-completion、improve-codebase-architecture、dev-env-setup、svg-draw (IconPark)

### 最终状态
- 技能总数：61 个
- 所有阶段执行成功

## 2026-05-22 开发环境重建（01:28）

### Skill 重新安装
重新安装 6 个 skill：
| Skill | 用途 |
|-------|------|
| skill-vetter | 安装前审查第三方技能安全性 |
| self-improving-agent | 持续自我学习和改进 |
| self-improving | 自我反思与错误修正 |
| humanizer | 去除AI写作痕迹，使文本更自然 |
| github | gh CLI 操作 GitHub |
| ui-ux-pro-max | UI/UX 专业设计能力 |

### SSH 密钥
- 生成 Ed25519 SSH 密钥对（openclaw@setone）
- 公钥已由用户手动添加到 GitHub

### 仓库配置
- 克隆 `setone` 仓库 `dev` 分支
- 路径：`/root/.openclaw/workspace/setone`
- 开发内容目录：desktop/

### 定时任务
- 设置 55 分钟后自动提交工作区变更（约 02:25）
- 流程：pull origin dev → 处理冲突 → add → commit → push 到 dev 分支

### 子代理
- 启动子代理更新聊天大记录和开发变更大记录

## 2026-05-23 开发变更（小希）

### 环境准备
- SSH密钥配置完成，用于访问 git@github.com:kkoo888/setone.git
- 仓库dev分支克隆完成（shallow clone）
- 工作目录：/root/.openclaw/workspace/setone/desktop

### 安装的技能
- skill-vetter（已有）
- self-improving-agent（已有）
- self-improving（新装）
- humanizer（新装）
- github（已有）
- ui-ux-pro-max（新装）

### 定时任务
- 设置22:33自动提交desktop文件夹变更到dev分支
- 流程：先pull → 处理冲突 → add desktop/ → commit → push

### 项目结构概览
desktop文件夹包含：
- src/ - 源代码
- modules/ - 模块目录
- skills/ - 技能目录
- tests/ - 测试目录
- themes/ - 主题目录
- dev-docs/ - 开发文档
- scripts/ - 脚本目录
- 配置文件：package.json, electron.vite.config.ts, tsconfig.json等
- 技术栈：Electron + Vite + TypeScript + Zustand

## 2026-05-23 - 环境准备与任务部署
- 生成SSH key并配置GitHub访问
- 安装6个技能包：skill-vetter, self-improving-agent, self-improving, humanizer, github, ui-ux-pro-max
- 拉取 setone 仓库 dev 分支，准备代码开发
- 设置定时提交任务（00:28自动提交desktop变更）
- 开始更新开发文档

## 2026-05-24 - 环境重新初始化与仓库配置

### SSH 密钥
- 重新生成 ed25519 SSH 密钥对（host: github.com-setone）
- 配置 ~/.ssh/config 使用专用密钥连接 GitHub

### 技能安装
- 通过 clawhub 安装/更新 6 个技能：
  - skill-vetter（技能审核）
  - self-improving-agent（自我改进代理）
  - self-improving（自我改进）
  - humanizer（人性化）
  - github（GitHub 集成）
  - ui-ux-pro-max（UI/UX 设计增强）

### 仓库配置
- 克隆 dev 分支 git@github.com:kkoo888/setone.git
- 工作目录：~/.openclaw/workspace/setone
- 重点关注 desktop 文件夹（Electron 桌面应用项目）
- git config: user.name=openclaw, user.email=openclaw@setone

### 定时任务
- 设置 55 分钟后自动提交 desktop 文件夹变更到 dev 分支
- 流程：先 pull --rebase → 处理冲突 → add desktop/ → commit → push

### 项目结构
desktop 文件夹包含：
- src/ - 源代码（main/renderer/preload/shared）
- modules/ - 23个功能模块
- skills/ - 技能配置
- tests/ - 测试文件
- themes/ - 主题文件
- dev-docs/ - 开发文档
- 技术栈：Electron 39 + React 19 + TypeScript 6 + Zustand + Vite 7

---

## 2026-05-24 - 环境重新初始化与仓库配置（第二轮）

### SSH 密钥
- 重新生成 ed25519 SSH 密钥对（host: github.com-kkoo888）
- 配置 ~/.ssh/config 使用专用密钥连接 GitHub

### 技能安装
- 通过 clawhub 安装/更新 6 个技能：github, humanizer, self-improving, self-improving-agent, skill-vetter, ui-ux-pro-max
- 本地安装 25 个技能（从 setone/skills/ 复制到 ~/.openclaw/skills/）
- 跳过 dev-env-setup

### 仓库配置
- 克隆 dev 分支 git@github.com:kkoo888/setone.git
- 工作目录：~/.openclaw/workspace/setone
- 重点关注 desktop 文件夹（Electron 桌面应用项目）
- git config: user.name=openclaw, user.email=openclaw@setone

### 定时任务
- 设置 55 分钟后自动提交 desktop 文件夹变更到 dev 分支（任务ID: ec5b8629）
- 流程：先 pull --rebase → 处理冲突 → add desktop/ → commit → push

### 项目结构
desktop 文件夹包含：
- src/ - 源代码（main/renderer/preload/shared）
- modules/ - 23个功能模块
- skills/ - 技能配置
- tests/ - 测试文件
- themes/ - 主题文件
- dev-docs/ - 开发文档
- 技术栈：Electron 39 + React 19 + TypeScript 6 + Zustand + Vite 7

## 2026-05-24 - 项目环境初始化（21:04）

**变更类型：** 环境配置
**变更内容：**
1. SSH密钥配置 - 新增ed25519密钥用于GitHub仓库访问
2. 技能安装 - 安装skill-vetter, self-improving-agent, self-improving, humanizer, github, ui-ux-pro-max
3. 仓库克隆 - 克隆setone仓库dev分支，工作目录为desktop/
4. 自动提交配置 - 设置55分钟后定时提交desktop/变更

## 2026-05-24 - 知识库下载器Bug修复 + 模型配置 + 技术调研

**变更类型：** Bug修复 / 配置变更 / 技术调研

**Bug修复：**
1. `modules/knowledge-base/services/DatasetDownloader.ts`
   - 修复：移除`.json`优先级匹配，避免`dataset_infos.json`被误选为数据文件
   - 修复：文件名从硬编码`datasetName.zip`改为使用ModelScope返回的真实文件名
   - 新增：元数据文件名黑名单（dataset_infos.json, dataset_dict.json等）
   - 改动：`resolveDownloadUrl`返回类型从`string|null`改为`{url, fileName}|null`

2. `src/renderer/src/stores/useSettingsStore.ts` - 温度还原为0.7（非项目配置）
3. `src/main/core/ai-service.ts` - 温度还原为0.7（非项目配置）

**配置变更：**
1. OpenClaw模型温度设置为0.2（`agents.defaults.models.xiaomi/mimo-v2.5-pro.params.temperature`）

**技术调研结论：**
1. 向量数据库现状：SQLite kb_chunks表 + 内存暴力搜索，未接入专业向量数据库
2. ChromaDB方案评估：JS客户端需REST服务，对Electron不友好，不推荐
3. **推荐方案：Qdrant** - Rust实现、支持嵌入式、HNSW索引、性能最优
4. Rust生态优化路线：Qdrant（P0）→ Rspack（P1）→ Biome（P1）→ Turso（P2）

## 2026-05-24 - Live2D5 渲染诊断变更

### 诊断日志添加
- AppModel.ts: drawModel 后像素读取 + FBO 状态检查
- AppModel.ts: drawModel 前 shader program 检查
- AppModel.ts: 检查所有关键 shader set (0,1,2,3,10)
- cubismrenderer_webgl.ts: drawMeshWebGL 内检查 shader program 和 shaderSet
- cubismrenderer_webgl.ts: 检查顶点坐标范围和 MVP 变换结果
- cubismrenderer_webgl.ts: 检查纹理绑定、culling、blend、index buffer

### 环境搭建
- SSH 密钥生成并添加到 GitHub
- 仓库 kkoo888/setone.git 克隆，dev 分支切换
- 聊天记录和开发变更记录更新

## 2026-05-24 开发环境重建

### SSH 密钥
- 生成 Ed25519 SSH 密钥对（xiaoxi-openclaw），用于 GitHub 认证

### 仓库克隆
- 克隆 setone 仓库 dev 分支
- 路径：/root/.openclaw/workspace/setone

### 技能安装
新增 29 个技能（从 setone/skills 复制）：
| 技能 | 用途 |
|------|------|
| brainstorming | 需求澄清、设计讨论（入口） |
| composition-patterns | React 组合模式 |
| diagnose | 硬 bug 深度诊断 |
| dispatching-parallel-agents | 并行任务分发 |
| executing-plans | 执行计划 |
| finishing-a-development-branch | 分支收尾 |
| grill-me | 方案压力测试 |
| grill-with-docs | 压力测试 + 更新文档 |
| handoff | 会话交接 |
| humanizer | 去除 AI 写作痕迹 |
| improve-codebase-architecture | 架构分析 |
| karpathy-guidelines | Karpathy 编码规范 |
| react-best-practices | React 性能优化 |
| react-view-transitions | View Transition API |
| receiving-code-review | 处理审查反馈 |
| requesting-code-review | 请求代码审查 |
| self-improving | 自我反思与改进 |
| subagent-driven-development | 子代理驱动开发 |
| systematic-debugging | 系统化调试 |
| tdd | 测试驱动开发 |
| test-driven-development | TDD 简洁版 |
| ui-ux-pro-max | 设计系统 |
| using-superpowers | 技能系统入门 |
| vercel-optimize | Vercel 部署优化 |
| verification-before-completion | 完成前验证 |
| web-design-guidelines | Web UI 审查 |
| writing-plans | 写实施计划 |
| writing-skills | 创建新技能 |
| zoom-out | 理解上下文 |

### 定时任务
- 设置 55 分钟后提醒提交 desktop 文件夹变更到 dev 分支
- 生成新的 Ed25519 SSH 密钥对（openclaw@setone-dev）
- 用于 GitHub 仓库认证

### 技能安装（31个）
从 setone/skills 批量安装，覆盖完整开发链路：
- 核心技能（14个）：brainstorming、zoom-out、grill-me、writing-plans、subagent-driven-development、requesting-code-review、verification-before-completion、finishing-a-development-branch、systematic-debugging、tdd、handoff、self-improving、humanizer、github
- React专用（5个）：react-best-practices、composition-patterns、web-design-guidelines、react-view-transitions、ui-ux-pro-max
- 按需选用（12个）：grill-with-docs、diagnose、improve-codebase-architecture、dispatching-parallel-agents、executing-plans、receiving-code-review、test-driven-development、writing-skills、vercel-optimize、self-improving-agent、skill-vetter、using-superpowers 等

### 仓库配置
- 克隆 setone 仓库 dev 分支
- Git user: OpenClaw <openclaw@setone-dev>
- 开发目录：desktop/
- 提交分支：dev

### 定时任务
- 设置 55 分钟后自动提交 desktop 目录变更并推送到 dev 分支

## 2026-05-24 新增 karpathy-guidelines 技能集成

### 变更概述
将 setone/skills 中的 karpathy-guidelines 技能集成到项目配置和工作流中。

### 文件变更
| 文件 | 变更 |
|------|------|
| `CLAUDE.md` | 技能数量 31→32，开发规范新增编码行为约束 |
| `skills/SKILLS-WORKFLOW.md` | 触发词索引+1、持续改进章节+1、核心技能表+1、一句话总结更新 |

### 技能定位
- 类型：元行为技能（全程生效）
- 触发词：简单点写、不要过度设计、karpathy、surgical、精准改动、先想清楚、别猜
- 归类：核心技能（和 self-improving 并列）
- 用途：写代码/审查/重构时自动叠加，防止过度设计、精准改动、目标驱动

### 补充修正
- skills 目录实际 33 个子目录，排除 dev-env-setup 后 32 个技能
- 修正 CLAUDE.md 子目录数量、SKILLS-WORKFLOW.md 中技能计数

### 数字修正
- karpathy-guidelines 归入核心技能（15个），按需选用恢复为 13 个
- 技能总数修正为 33 个（CLAUDE.md、SKILLS-WORKFLOW.md 各处统一）

### 最终修正
- 移除不存在的 request-refactor-plan、design-an-interface（目录不存在）
- 按需选用 11 个，核心 15 个，总数 32 个
- 清理触发词索引和工作流中的残留引用

### karpathy-guidelines 触发词补全
- 逐行核对 SKILLS-WORKFLOW.md，确认一致性
- 新增 8 个触发词覆盖编辑和修 bug 场景：改代码、修代码、最小改动、只改这个、edit code、fix bug、fix it、surgical edit

### karpathy 触发词追加
- 新增 15 个触发词，总计 74 个
- 覆盖：不对、搞清楚、错误信息、核对下、检查下、逐行、认真、仔细、确定吗、加个功能、重构下、优化下、有没可优化的、有没更好的、冗余

## 2026-05-24 SKILLS-WORKFLOW.md 工作流优化

### 4 处优化
| # | 位置 | 改动 |
|---|------|------|
| 1 | 快速决策表 | 新增 karpathy + verification vs systematic-debugging 判断行 |
| 2 | 新功能开发链路 | 执行/审查环节标注叠加 karpathy-guidelines |
| 3 | 修 Bug 链路 | 要点新增"先写复现测试（karpathy Goal-Driven）" |
| 4 | 一句话总结 | 修 bug 行补 tdd（先写复现测试，定义成功标准） |
| 5 | 重构链路 | 执行/审查环节标注叠加 karpathy-guidelines |

## 2026-05-25 Cubism 5 渲染流程文档与 live2d-5 模块代码审查修复

### Cubism5渲染流程.md — 重写为完整参考文档
- 基于官方 CubismWebFramework（develop 分支，5-r.5 版本）校验
- 10 个章节、820 行：渲染管线总览、矩阵乘法语义、模型坐标系、5-r.5 混合模式系统、Offscreen 渲染、遮罩系统、GL 状态管理、Shader 文件体系、ModelColor 计算、版本兼容性
- 版本号统一修正为 5-r.x 格式
- RendererProfile 标注为"本项目不需要开启"（宠物窗口独立 Canvas）

### live2d-5 模块代码修复（对照规范）

#### cubism5-service.ts
| 修复项 | 原代码 | 修复后 |
|--------|--------|--------|
| blendFunc 错误 | `gl.blendFunc(SRC_ALPHA, ONE_MINUS_SRC_ALPHA)` | 删除，由 Cubism preDraw 管理 |
| 每帧 GL 状态污染 | `gl.enable(BLEND)` + `gl.blendFunc` 每帧执行 | 只保留 `gl.clear()` |
| clearColor | `(0.12, 0.12, 0.2, 1)` 不透明 | `(0, 0, 0, 0)` 透明背景 |
| MVP fallback | 不存在的正交投影 fallback + 诊断日志 | 精简为纯逻辑 |
| _renderFrameCount | 前 5 帧打印 shader 状态、GL version 等 | 全部删除 |
| CubismShaderManager_WebGL import | 手动管理 shader | 删除，SDK 自动处理 |

#### AppModel.ts
| 修复项 | 原代码 | 修复后 |
|--------|--------|--------|
| CubismLook 参数 | 手动创建 6 个 LookParameterData 硬编码 | `CubismLook.create()` 默认参数 |
| CubismBreath 参数 | 手动创建 5 个 BreathParameterData 硬编码 | `CubismBreath.create()` 默认参数 |
| sortUpdatableList | setupEffectIds 末尾手动排序 | 删除，添加顺序已正确 |
| Shader 预热 | `loadShaders()` + 5 秒轮询等待 + 超时抛异常 | 删除，SDK 自动处理 |
| render() 诊断 | readPixels + getParameter + getError 前 5 帧执行 | 全部删除 |
| 无用 import | CubismRenderer_WebGL, CubismShaderManager_WebGL, CubismFramework, CubismDefaultParameterId | 全部删除 |
| 无用变量 | _shaderWaitTimers, _firstFrameLogged, _diagFrameCount | 全部删除 |

#### types.ts
- 删除 7 个旧版遗留接口：Cubism3FileReferences, Cubism3ModelJson, CubismFrameworkLike, CubismMocLike, CubismModelLike, CubismModelInternalLike, CubismRendererLike
- 删除未使用的 Live2D5PetState 接口
- 168 行 → 66 行

#### Live2D5PetPage.tsx
- 删除未使用的 hitAreas state 变量

### 净减约 200 行，无新增代码，全是删减和简化
## 2026-05-25 知识库模块 Vectra 向量数据库集成

### 依赖变更
| 操作 | 包名 | 版本 | 说明 |
|------|------|------|------|
| 新增 | vectra | ^0.14.0 | 轻量级本地向量数据库 |
| 新增 | pdf-parse | ^2.4.5 | PDF 文本提取 |
| 新增 | mammoth | ^1.12.0 | DOCX 文本提取 |

### 文件变更
| 文件 | 操作 | 说明 |
|------|------|------|
| modules/knowledge-base/services/VectraStore.ts | 新建 | Vectra 封装，向量+内容+元数据一站管理 |
| modules/knowledge-base/services/VectorStore.ts | 重写 | 移除手动余弦相似度，改用 Vectra 搜索 |
| modules/knowledge-base/services/EmbeddingService.ts | 重写 | embedBatch 改 5 并发，新增 getModel() |
| modules/knowledge-base/services/TextExtractor.ts | 修改 | 新增 PDF/DOCX 提取支持 |
| modules/knowledge-base/index.ts | 修改 | 注入 VectraStore，新增 kb_reindex 能力 |
| modules/knowledge-base/module.json | 修改 | 新增 kb_reindex 工具声明 |

### 架构变化
**旧流程（N+1 查询）：**
```
搜索 → Vectra查向量 → SQLite查每个片段内容 → SQLite查文档信息
```
**新流程（1 次查询）：**
```
搜索 → Vectra返回向量+内容+元数据 → 补文档名 → 完事
```

**职责划分：**
- SQLite → 只存文档元数据（列表/统计）
- Vectra → 向量 + 片段内容 + 元数据，一站管理

### 新增能力
| 能力 | 说明 |
|------|------|
| kb_reindex | 换嵌入模型后一键重建索引，删除旧向量→用新模型重新向量化 |
| PDF 提取 | 支持 .pdf 文件导入知识库 |
| DOCX 提取 | 支持 .docx 文件导入知识库 |
| 并发嵌入 | embedBatch 从串行→5并发，大文件导入速度提升约 5 倍 |

---

## 2026-05-25 02:05 - live2d-5 诊断修复 + knowledge-base 目录重构

### 变更文件
| 文件 | 操作 | 说明 |
|------|------|------|
| modules/live2d-5/lib/motion/cubismmotionqueuemanager.ts | 修改 | 3 行变更 |
| modules/live2d-5/lib/rendering/cubismrenderer_webgl.ts | 修改 | 99 行精简（清理诊断代码） |
| modules/live2d-5/services/cubism5-service.ts | 修改 | 17 行变更 |
| modules/knowledge-base/index.ts | 修改 | 新增原始文件目录/索引目录解析逻辑 |

### 知识库目录重构（knowledge-base/index.ts）
- 新增 `rawDir` 和 `indexDir` 配置解析
- 原始文件目录（rawDir）：空时默认 `dataDir/datasets`，支持绝对路径和相对路径
- 索引目录（indexDir）：空时默认 `dataDir/vectra-doc-index`，支持绝对路径和相对路径
- VectraStore 从 `context.dataDir` 改为使用 `indexDir`
- DatasetDownloader 从 `context.dataDir` 改为使用 `rawDir`

### 提交记录
- `587b6bd` debug(live2d-5): 在 updateModel 崩溃点添加精准诊断
- `8f1e29a` fix(live2d-5): 修复 _motions null 崩溃根因 + 清理 90 行诊断代码
- `1a5d992` fix(data-downloader): 下载完成自动导入知识库
- `3fbc447` fix(data-downloader): 文件名加数据集前防覆盖
- `07975ce` auto: desktop updates（knowledge-base 目录解析 + live2d 修复）
