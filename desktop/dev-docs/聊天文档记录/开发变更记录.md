# 📝 开发变更记录

> 本文件记录每日开发变更，按日期倒序排列。  
> 格式：日期 → 变更类型 → 具体内容 → 负责人

---

## 2026-05-18

### 🏗️ 环境初始化

| 变更项 | 详情 | 状态 |
|--------|------|------|
| 技能安装 | 安装缺失技能 `self-improving`、`humanizer`，补齐至 5/5 | ✅ 完成 |
| Git 配置 | 全局设置 user.name=kkoo888, user.email=kkoo888@users.noreply.github.com | ✅ 完成 |
| 仓库克隆 | 克隆 `setone` 到 `/root/.openclaw/workspace/setone/`，切换至 `dev` 分支 | ✅ 完成 |

### 📋 角色与任务

| 变更项 | 详情 | 状态 |
|--------|------|------|
| 角色设定 | 设定 AI 助手为"主人的温柔体贴的专属秘书兼开发总监" | ✅ 完成 |
| 开发规范同步 | 确认 `CODING_STANDARD.md`（P3C → TypeScript 适配）为编码标准 | ✅ 完成 |
| 定时任务 | 设置 45 分钟后自动提交 dev 分支并合并到 main | ✅ 完成 |

### 📄 文档变更

| 变更项 | 详情 | 状态 |
|--------|------|------|
| 聊天记录归档 | 创建 `2026-05-18-会话记录.md`、`2026-05-18-小希初始化与任务分配.md` | ✅ 完成 |
| 聊天记录整合 | 创建 `2026-05-18_聊天记录.md`（结构化综合文档） | ✅ 完成 |
| 变更记录文件 | 创建本文件 `开发变更记录.md` | ✅ 完成 |

### ⚠️ 安全事项

| 事项 | 处理 |
|------|------|
| GitHub token 泄露 | 主人在聊天中直接提供了 token，已提醒尽快撤销并重新生成 |

### 🐛 Bug 修复（6项）

| 变更项 | 详情 | 涉及文件 |
|--------|------|---------|
| 日历提醒重复触发 | ReminderChecker 增加 notifiedIds 标记，同一事件只通知一次 | `reminder-checker.ts`, `calendar/index.ts` |
| EventMap 缺事件定义 | 补充 `notify` 和 `theme:changed` 类型 | `event.ts` |
| clipboard require 重复 | activate 时缓存 electron 引用，三个工具方法统一使用缓存 | `clipboard-history/index.ts` |
| 剪贴板轮询太频繁 | 2秒 → 5秒，降低 CPU/电量消耗 | `clipboard-history/index.ts` |
| execSync 阻塞主进程 | getDiskUsage() 改为 exec + Promise 异步 | `system-info-service.ts` |
| module.json 缺事件声明 | calendar 加 notify，theme-store 加 theme:changed | 两个 `module.json` |

### 🎨 UI 样式优化（10个页面）

| 页面 | 样式文件 | 设计亮点 |
|------|---------|---------|
| 日程日历 | `pages/calendar.css` | 卡片式日历、今日高亮、事件条微动效 |
| 剪贴板历史 | `pages/clipboard.css` | 渐变遮罩、hover 显示操作、固定分组 |
| 系统仪表盘 | `pages/dashboard.css` | 渐变进度条闪光、状态脉冲、卡片悬浮 |
| 主题商店 | `pages/theme-store.css` | 渐变预览区、毛玻璃徽章、安装/应用状态 |
| 代码片段 | `pages/snippets.css` | 编辑器卡片、语言标签、代码块样式 |
| 知识库 | `pages/knowledge-base.css` | Tab 切换、文档列表、搜索/问答结果 |
| 多会话 | `pages/sessions.css` | 活跃会话高亮、hover 显示操作 |
| 通知中心 | `pages/notifications-preview.css` | 类型图标色彩、未读标记、hover 操作 |
| 快捷键 + 翻译 | `pages/shortcuts-translator.css` | 键帽样式、双栏翻译、swap 按钮 |
| 工作流 | `pages/workflow.css` | 卡片布局、运行状态指示、日志列表 |

所有页面均适配暗色主题 + 响应式布局。

### 🔌 前后端断联修复（4项）

| 模块 | 问题 | 修复方案 | 涉及文件 |
|------|------|---------|---------|
| KnowledgeBase | 类型文件不存在，编译报错 | 创建 `types/knowledge-base.ts` | 新增文件 |
| SystemDashboard | `module_list` 工具名不对 | 改为 `module:list` + 兼容数组返回 | `SystemDashboardPage/index.tsx` |
| WorkflowPage | `workflow_create_from_template` 不存在 | 改用 `workflow_create` + 模板数据构造 | `WorkflowPage/index.tsx` |
| NotificationsPage | 后端无通知管理，4个工具全空 | **新建通知管理模块**（SQLite 持久化 + eventBus 监听） | `modules/notifications/` 新增 |

### 🔧 快捷指令修复

| 问题 | 修复 |
|------|------|
| ShortcutsPage 纯本地 state，快捷键未注册到系统 | 重写页面，连接后端 HotkeyService，支持注册/注销/启用/禁用/编辑/持久化 |
| 缺少 `hotkey_list` / `hotkey_unregister` 工具 | desktop-integration 模块新增两个工具 |

---

## 2026-05-17

> 以下为从项目历史中提取的变更记录。

### 🐛 Bug 修复

| 变更项 | 详情 |
|--------|------|
| 打包启动失败 | 修复 NSIS 安装包打包后无法启动问题 |
| 模块入口丢失 | 修复模块入口文件找不到问题（modules-dist 编译产物） |
| CronParser 注释 | 修复 CronParser JSDoc 注释问题 |
| clean.js 权限 | 修复打包时 dist-packaged 权限问题 |
| 模块加载诊断 | 模块加载诊断日志增强 + 状态映射 bug 修复 |

### ✨ 新增功能

| 变更项 | 详情 |
|--------|------|
| 工作流模块 | 新增 workflow 模块 — 8 个文件，6 个工具 |

### 🔀 分支合并

| 操作 | 详情 |
|------|------|
| dev → main | 合并 Live2D canvas 修复 |

---

> 📌 **使用说明**：每日新增变更请添加在对应日期下，保持倒序排列。  
> 变更类型图标：🏗️ 环境配置 / 🐛 Bug 修复 / ✨ 新增功能 / 🔀 分支合并 / 📄 文档 / ⚠️ 安全
