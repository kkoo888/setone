# 📚 聊天大记录

> 记录所有重要聊天会话的关键信息与决策

---

## 📝 2026-05-21 00:56~01:00 聊天记录

### 用户指令
- 用户要求新建 SSH 密钥并展示，用于添加到 git 仓库 dev 分支 (git@github.com:kkoo888/setone.git)
- 安装 5 个 skill：skill-vetter, self-improving-agent, self-improving, humanizer, github
- SSH 公钥已生成并展示给用户
- 5 个 skill 全部安装成功
- 用户确认 SSH 密钥已添加到 GitHub
- 成功拉取 setone 仓库 dev 分支
- 项目结构：desktop/ (Electron + Vite + TypeScript 桌面应用), dev-docs/ (开发文档), skills/
- 设置定时任务：55 分钟后自动提交 desktop 文件夹变更到 dev 分支
- 开子代理更新聊天记录

### 关键信息
- 仓库地址：git@github.com:kkoo888/setone.git
- 分支：dev
- 工作目录：/root/.openclaw/workspace/setone
- 定时提交任务 ID：9f866070-9f72-4cf2-8533-2be9538a9d82
- SSH 密钥：ed25519, fingerprint: SHA256:WJrlu6sFp1RFLLSNFwgtZcDC6LDCuZVhqWlpfJm7dyM

---

## 📝 2026-05-21 01:05~01:25 聊天记录

### 用户指令
- 用户发现提交记录中图标更改不生效，要求排查原因
- 排查发现 KnowledgeBasePage 和 WorkflowPage 中 STATUS_ICONS 被错误用在字符串拼接中，变成 [object Object]
- 用户要求将所有状态图标和消息统一整理到一个公共模块
- 创建 `src/renderer/src/utils/statusMessages.ts` 作为唯一图标+消息源
- 用户要求统一修好所有页面，统一格式规范，移除不合规写法
- 修复 KnowledgeBasePage（10处）和 WorkflowPage（1处）
- 用户要求将此方式写入模块开发规范文档
- 新建 `dev-docs/30-图标与状态消息开发规范.md`
- 更新索引文档 `00-索引-开发路线图.md` 和 UI 框架文档 `12-主窗口UI框架.md`

### 关键决策
- `IconMap.tsx` 保留为 re-export 兼容层，避免大量页面修改 import 路径
- `setMessage()` 统一用 `Msg.*`（emoji 字符串），JSX 模板统一用 `{STATUS_ICONS.*}`
- 新增开发规范文档编号 30，与现有 29 个文档体系一致
