# 35-多会话模块（multi-session）

> **前置依赖**：无
> **版块**：35
> **说明**：多对话会话管理，支持创建、切换、删除、重命名、固定会话

---

## 模块元数据

| 字段 | 值 |
|------|-----|
| ID | `multi-session` |
| 版本 | 1.0.0 |
| 作者 | setone |
| 依赖 | 无 |
| 优先级 | 10 |

## 能力声明（provides）

| 类型 | 名称 | 说明 |
|------|------|------|
| tool | `session_list` | 列出所有会话 |
| tool | `session_create` | 创建新会话 |
| tool | `session_switch` | 切换会话 |
| tool | `session_delete` | 删除会话 |
| tool | `session_rename` | 重命名会话 |
| tool | `session_pin` | 固定/取消固定会话 |

## 实现状态

✅ **已实现**（448 行代码）：
- 会话 CRUD 操作
- 会话切换和固定
- 会话列表管理
